// Event listeners for all navigation tabsdocument.getElementById('gallery-tab').addEventListener('click', function(event) {    event.preventDefault();  // Prevent default link behavior    showSection('gallery');});document.querySelector('a[href="#stories"]').addEventListener('click', function(event) {    event.preventDefault();  // Prevent default link behavior    showSection('stories');});document.querySelector('a[href="#about"]').addEventListener('click', function(event) {    event.preventDefault();  // Prevent default link behavior    showSection('about');});// Function to show the correct section and hide the othersfunction showSection(sectionId) {    const sections = ['stories', 'gallery', 'about'];    // Hide all sections    sections.forEach(section => {        document.getElementById(section).style.display = 'none';    });    // Show the selected section    document.getElementById(sectionId).style.display = 'block';}